'use client';

import { ValueState } from '../../enums/index.js';
import { withWebComponent } from '../../internal/withWebComponent.js';
/**
 * The `Select` component is used to create a drop-down list. The items inside the `Select` define the available options by using the `Option` component.
 *
 * __Note:__ This component is a web component developed by the UI5 Web Components’ team.
 *
 * [UI5 Web Components Storybook](https://sap.github.io/ui5-webcomponents/playground/?path=/docs/main-Select)
 */
const Select = withWebComponent('ui5-select', ['accessibleName', 'accessibleNameRef', 'name', 'valueState'], ['disabled', 'required'], ['valueStateMessage'], ['change', 'close', 'open']);
Select.displayName = 'Select';
Select.defaultProps = {
  valueState: ValueState.None
};
export { Select };